{
  "name": "mindvault-ai",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace @mindvault-ai/backend",
    "dev:frontend": "npm run dev --workspace @mindvault-ai/frontend",
    "build:backend": "npm run build --workspace @mindvault-ai/backend",
    "build:frontend": "npm run build --workspace @mindvault-ai/frontend"
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
